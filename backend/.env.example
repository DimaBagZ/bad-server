# ===========================================
# ПРИМЕР ФАЙЛА ОКРУЖЕНИЯ ДЛЯ WEBLAREK BACKEND
# ===========================================

ORIGIN_ALLOW=http://localhost:5173,http://127.0.0.1:5173,http://localhost
# Настройки сервера
PORT=3000
NODE_ENV=development

# База данных
# Для локальной разработки:
DB_ADDRESS=mongodb://127.0.0.1:27017/weblarek?authSource=admin
# Для Docker:
# DB_ADDRESS=mongodb://root:example@mongo:27017/weblarek?authSource=admin

# JWT токены (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ В ПРОДАКШЕНЕ!)
# Используйте криптографически стойкие случайные строки длиной минимум 32 символа
AUTH_ACCESS_TOKEN_SECRET=token-key
AUTH_REFRESH_TOKEN_SECRET=refresh-token
AUTH_ACCESS_TOKEN_EXPIRY=10m
AUTH_REFRESH_TOKEN_EXPIRY=7d

# CSRF защита (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ В ПРОДАКШЕНЕ!)
CSRF_SECRET=secret-key

# Загрузка файлов
UPLOAD_PATH=images
UPLOAD_PATH_TEMP=temp

# CORS настройки
# ORIGIN_ALLOW=http://localhost:5173